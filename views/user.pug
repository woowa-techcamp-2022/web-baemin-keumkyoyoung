extends layout

block content
    header(class=" mo-header")
        a(href="/" class="left-arrow flex-center items-center" ,role="button" )
          img(src="/images/arrow-left.png" , class="left-arrow" alt="", srcset="")
        h1.title= title 
        button.next-btn(href="/" ,role="layout" disabled) 완료

    form(class="phone-form")
        div.email.items-end.input-controller.flex(data-input-id="email")  
                label.flex-1 
                    div 이메일
                    input(id="email" ,type="email" ,name="email")
                    div.check-icon
                    div.x-icon
                button.duplicate-btn 중복확인

             
            
        div.input-controller(data-input-id="nickname")  
            label 
                div 닉네임
                input(id="nickname" ,type="text" ,name="nickname", )
            div.check-icon
            div.x-icon
        div.input-controller.has-error-message(data-input-id="password")  
            label 
                div 비밀번호
                input(id="password" ,type="tel" ,name="password", maxlength="13")
            div.check-icon
            div.x-icon
        p(class="error-msg .js-id-empty-msg") 비밀번호를 입력해주세요
        div.input-controller(data-input-id="birth")  
            label 
                div 생년월일
                input(id="birth" ,type="number" ,name="birth", pattern="[0-9]*")
            div.check-icon
            div.x-icon
    script(src="/scripts/phone.js",type="module") 
    